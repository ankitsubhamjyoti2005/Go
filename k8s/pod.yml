apiVersion: v1
kind: pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
    env: demo
    type: frontend
spec:
  containers:
  - name: nginx-container
    image: nginx:1.15.7
    ports:
    - containerPort: 80
